import { READ_EVENTS } from "../actions/index";

// export const getAxios = (state = {}, action:void) => {
//   switch (action.type) {
//     case READ_EVENTS:
//       return action;
//     default:
//       return state;
//   }
// };
